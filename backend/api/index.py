"""Точка входа для функций Vercel.

Vercel ищет в этом файле объект ASGI с именем ``app`` и вызывает его на каждый запрос.
Больше здесь ничего быть не должно: всё, что относится к приложению, живёт в ``app`` и
одинаково работает и в облаке, и под uvicorn на сервере агентства (ADR-0028).

Регион и ограничения функции заданы в ``vercel.json``: Франкфурт — ближайшая к Ташкенту
площадка из разрешённого перечня стран.
"""

from app.main import app

__all__ = ['app']
