import '@testing-library/jest-dom/vitest';

// Локализация инициализируется для всех тестов так же, как в приложении. Без неё
// useTranslation возвращает ключи вместо текста, и проверки по видимой надписи
// падают по причине, не имеющей отношения к проверяемому поведению.
import './shared/i18n';
