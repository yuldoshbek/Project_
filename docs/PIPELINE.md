# Процесс разработки ORBITA

Пайплайн реализован проектными командами (`.claude/commands`) и скиллами
(`.claude/skills`) — работает без внешних плагинов.

```
/council                        делать ли это вообще, вариант A или B
    │                           → docs/council/, docs/adr/
    ▼
/grill <раздел>                 допрос требования до конца
    │                           → docs/CONTEXT.md, docs/adr/, docs/OPEN-QUESTIONS.md
    ▼
/to-spec → /to-tickets          спецификация и тикеты с зависимостями
    │                           → docs/SPEC.md, docs/tickets/
    ▼
using-git-worktrees             дерево на тикет
+ dispatching-parallel-agents   раздать волну тикетов агентам
    ▼
/tdd ORB-NNN → /implement       по одному тикету за заход, /clear между тикетами
    ▼
/verify                         проверка фактами, а не ощущением
+ /code-review                  встроенная команда Claude Code
    ▼
/finish-branch                  слияние, уборка дерева, обновление документов
```

## Что уже пройдено

| Шаг | Результат |
|---|---|
| `/council` | [COUNCIL-0001](council/COUNCIL-0001.md): своя разработка; «A-ready» вместо выбора A или B |
| `/council` ещё раз | [COUNCIL-0002](council/COUNCIL-0002.md): объём сузился до двух пользователей. Снято три тикета, сокращено четыре, добавлен эпик E5 |
| `/grill` | [CONTEXT.md](CONTEXT.md) (раздел 4 — десять разобранных неоднозначностей ТЗ), ADR-0001…0010, [OPEN-QUESTIONS](OPEN-QUESTIONS.md) с 14 вопросами |
| `/to-spec` | [SPEC.md](SPEC.md): модель данных, API, экраны, НФТ в проверяемых формулировках |
| `/to-tickets` | 62 тикета в [docs/tickets](tickets), из них 56 к работе; порядок и зависимости пересобраны под новый объём |

## Что дальше

1. Согласовать с заказчиком [OPEN-QUESTIONS](OPEN-QUESTIONS.md) — в первую очередь Q15
   (допустима ли выгрузка в Google), Q17 (правка ТЗ под новый объём), Q3 (бланки отчётов).
2. Волна 2: `/implement ORB-003`, затем ORB-004.
3. Дальше по порядку из [tickets/INDEX.md](tickets/INDEX.md).

## Правила, которые нельзя нарушать в процессе

- **Один тикет — одна ветка — одно рабочее дерево.**
- **`/clear` между тикетами.** Контекст прошлого тикета мешает следующему.
- **Готовность — это `/verify`,** а не «код написан».
- **В одной волне миграции создаёт один тикет.** Иначе расходятся головы Alembic.
- **Ни одного ключа в репозитории.** Он публичный: токен бота и секрет Google — только
  в окружении и GitHub Secrets.
- **Неотвеченный вопрос не угадывается:** реализуем по допущению из OPEN-QUESTIONS и
  называем это вслух.
