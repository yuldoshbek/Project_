# Аудит ORBITA 25.09.2026 — состояние, диагноз, путь к завершению

Проверено по коду и по GitHub, а не по документам: ветки `block-0-foundation` (25e8294) и
`block-1-core` (2421602), `main` (225854c), настройки репозитория, прогоны Actions,
локальный запуск на машине разработки. Предыдущий аудит — [20.09](AUDIT-2026-09-20.md).

## 1. Итог

1. **За 17 дней — шесть пересборок объёма, ни одного рабочего экрана у руководителя.**
   35 ADR (10 отменены, 4 прожили 2–4 дня), ≈630 КБ документов против ≈1 700 строк кода
   интерфейса. Живой раздел один — «Управление», и на текущей ветке он падает.
2. **Последняя пересборка (20.09) заложила правильное основание** — ТЗ v2.0, план из
   четырёх блоков, стек ADR-0030, доступ по ссылке, `services/metrics.py`, разбор Ижро —
   **и брошена посередине.** `block-1-core` красная, пять дней без коммитов.
3. **Конвейер ни разу не работал в облаке.** PR нет, CI на ветках блоков не запускался,
   `main` красный с 16.09, секретов и правил ветки нет, в workflow пять ошибок (раздел 4).
   «Блок 0 зелёный» — это прогон на одной машине.
4. **Инструкция заказчика 19.09 выполнена в части архитектуры и стека, но треть её
   пунктов ТЗ v2.0 прямо запрещает** (раздел 5). Это развилка, которую надо закрыть
   решением, а не очередной пересборкой.
5. **Главная болезнь — не код, а пересборки.** Код ядра в целом добротный. Путь к
   завершению — заморозить ТЗ, вернуть ветку в зелёное и довести Пульт до телефона
   руководителя.

## 2. Хронология смен объёма

| Дата | Что сменилось | Чем заплачено |
|---|---|---|
| 08.09 | ТЗ v1.0, 80 тикетов → в тот же день «система на двоих» | сняты 3 тикета, ADR-0003 |
| 10.09 | SETA, риски, обращения, встречи, телефон — SPEC 3.0, эпик E6 | 16 тикетов, позже все сняты |
| 16.09 | договор с SETA, тёмная «космическая» тема основной | тема перекрашена |
| 17.09 | пересборка: 98 карточек, эпик E7 | 16 карточек сняты |
| 18.09 | «три блока», снят вход, отменены ADR-0012/17/18/22/23 | 98 карточек в архив, −2 779 строк входа |
| 20.09 | ТЗ v2.0, план v3, новый стек, одна базовая миграция | −20 367 строк; выброшены доска проектов, база задач, мобильный каркас, формы |
| 20.09 вечер | аудит: база — «фундамент под ТЗ v1.0, переименованный» → шаг 0 блока 1 | ещё −7 447 строк, API сведён к справочникам |
| 21–25.09 | тишина, ветка сломана | — |

## 3. Что сделано сегодня и что показали проверки

- Локальная копия была на ветке `orb-017-attachments` от 12.09 с незакоммиченным дублем
  ORB-017 (35 из 38 файлов побайтно равны влитому `aaf104d`). Отложено в
  `stash@{0}`, работа переключена на `block-1-core`.
- **Старый `.env` указывал на порт 55432** — там живёт база ORBITA v1 (и, по документам,
  может жить база SETA). Заменён новым по `.env.example`; старый — `.env.v1-backup`.
- База v3 поднята отдельным томом (`COMPOSE_PROJECT_NAME=orbita3`, порт 55433): том v1
  `orbita_postgres-data` не тронут. Контейнеры v1 (postgres, redis, minio, clamav,
  gotenberg) по-прежнему запускаются вместе с Docker.
- `make doctor` — всё на месте. Миграция применяется.

| Проверка на `block-1-core` | Итог |
|---|---|
| `import app.main` | OK |
| import-linter | 2 контракта соблюдены |
| ruff | 2 ошибки, 6 файлов не отформатированы |
| mypy | 12 ошибок, все в `seed.py` |
| `alembic check` | **красный**: 7 таблиц и десятки столбцов моделей нет в миграции |
| pytest | **не собирается**: `tests/conftest.py:37` импортирует `seed.py`, тот падает на импорте |
| tsc, eslint, vitest | зелёные, 45 тестов |
| e2e | «Управление» падает: `ManagementSection.tsx:160` ждёт удалённые `priorities` |

## 4. Критичные дефекты (проверены по коду)

| # | Где | Дефект | Последствие |
|---|---|---|---|
| 1 | `services/metrics.py:270,345,346,399` | `.date()` от времени в UTC | срок 26.09 03:00 по Ташкенту считается просроченным утром 26.09 |
| 2 | `domain/attention.py:180` | «горит» сортируется по `-deviation`, а отклонение — дни до срока | «горит через 6 дней» выше «горит сегодня» |
| 3 | `services/metrics.py:174-179` | «ждёт решения» наполняется решениями «вернуть», которые домен считает закрывающими | верхняя ступень лестницы показывает не то |
| 4 | `services/metrics.py:250-274` | признак жизни проекта не учитывает сам проект | новый проект без задач сразу «молчит» |
| 5 | `observability.py:139-143` | путь запроса пишется в лог целиком | личная бессрочная ссылка `/api/access/{token}` попадает в журналы |
| 6 | `api/routes/access.py:72` | вход — GET без промежуточной страницы | предпросмотр ссылки в мессенджере сам открывает сессию на 30 дней |
| 7 | `api/errors.py` | нет сопоставления `StaleDataError`, `IntegrityError` | конфликт версий отдаётся как 500, инвариант 15 не выполнен |
| 8 | `settings.py:80`, `Makefile:25` | порт по умолчанию 55432 | без `.env` тесты идут в чужую базу |
| 9 | `.github/workflows/jobs.yml:21,66` | cron `0 4-14 * * 1-6`, а `case` ждёт `0 * * * *` | ежечасная проверка сроков никогда не запускается |
| 10 | `.github/workflows/deploy.yml` работа `keys` | нет `environment: production` | проверка ключей всегда падает, выкладка не начнётся |
| 11 | `backup.yml` + `scripts/neon.sh` | строка подключения у ветки без вычислителя | ночная копия почти наверняка падает |
| 12 | `preview.yml`, `preview-comment.md` | первую ссылку никто не выпускает | в превью нельзя войти |
| 13 | `frontend/scripts/check-budget.mjs` | считает весь JS, а не начальный | любой отложенный кусок (сцена, графики) валит сборку |
| 14 | `frontend/src/main.tsx:28` | опрос раз в 15 с на все запросы | ≈340 тыс. вызовов в месяц на двоих, база не засыпает |
| 15 | `domain/cycles.py:55,66` | горизонт включает оба конца, кварталы с 29–31 теряются | ежегодный цикл даёт две даты |

Полный список поломок шага 0 (28 пунктов: модели против миграции, seed, тесты, контракты
справочников) — рабочая заметка к шагу 0, переносится в чек-лист коммита.

## 5. Инструкция 19.09 против ТЗ v2.0

| Пункт инструкции | Состояние |
|---|---|
| Стек: React, shadcn/ui, Tailwind, Motion, R3F, Lucide, Recharts | React 19, Tailwind 4, Lucide — есть. **shadcn не поставлен**, Motion и Recharts не используются, three/R3F нет, шрифт не загружается |
| Тема «космос / мониторинг» | **противоречие**: ТЗ v2.0 и ADR-0030 — светлая основная + приглушённая; космос — полоса 2 px |
| Единая архитектура, CLAUDE.md | сделано 20.09 и нарушено в тот же день (шаг 0) |
| Статусы и статистика проектов, графики | лестница внимания вместо счётчиков; ТЗ запрещает «метрики, которые только растут» |
| Календарь, вехи | блок 1 |
| Приоритеты, зависимости задач, burndown | приоритеты удалены; зависимостей и burndown нет ни в ТЗ, ни в схеме |
| Данные бота, время по задачам, отчёты день/неделя/месяц | **запрещено ТЗ**: собственный бот и трудозатраты — «не делаем»; отчёт недели и месяца — блок 1, дневного нет |
| Канбан с блокерами | доска только у проектов (блок 1); у задач доски и поля «блокер» нет |
| Самоорганизующаяся загрузка данных | только таблицы Ижро фиксированной формы (блок 2); ТЗ запрещает поля «на будущее» |
| Реальное время, «что если» | «что если» — блок 1, но без зависимостей влияет только на сам объект; «живые правки» — опрос |
| Мобильная сводка | Пульт — блок 1, сейчас заглушка |

## 6. Что оставить, исправить, сделать

**Оставить без переписывания:** `domain/ijro.py` (кроме переноса года и `NULLS NOT DISTINCT`
в ключе идемпотентности), `api/transaction.py`, `services/audit.py`, `services/access.py`,
`domain/access.py`, `jobs/registry.py`, `alembic/env.py`, изоляцию тестов, контракты
import-linter; во frontend — токены и проверку контраста, тему, API-клиент, i18n, оболочку
трёх устройств, линтеры.

**Исправить:** дефекты раздела 4; shadcn/ui вместо четырёх самописных компонентов;
разбиение сборки по маршрутам; метка изменений вместо опроса всего; `.claude/launch.json`
(`npm` без `.cmd` на Windows не стартует); цель `dev` в `make.ps1`.

**Сделать:** экраны блока 1 — Пульт, Программы, Проекты, Задачи, Календарь, Захват.

## 7. План ускорения

| Этап | Результат | Оценка |
|---|---|---|
| A. Шаг 0 до зелёного | исправленные показатели, одна базовая миграция, seed из указов, тесты, «Управление» под новые справочники | 1–2 дня |
| B. Конвейер вживую | пять ошибок workflow, CI на push в `block-*`, PR блока 0 в `main`, заказчик проходит SETUP (≈30 мин), первое превью, замер холодного старта | 0,5–1 день + решение по тарифу (V10) |
| C. Пульт на демо-данных | лестница внимания и решение в одно касание на iPhone, космический слой уровня 0 | 3–4 дня |
| D. Остальное ядро | Проекты (доска, таблица, таймлайн, «что если»), Задачи, Программы, Календарь, сводка в 08:30 | 8–12 дней |
| **До продукта в руках руководителя** | конец блока 1 | **≈2,5–3,5 недели** |

**Правила, которые снимают трение процесса:**

1. Объём ТЗ v2.0 заморожен до приёмки блока 1. Новое — в список приёмки.
2. Новый ADR — только когда решение отменяет действующее. Остальное — строка в плане.
3. Готово — это зелёный CI на GitHub, а не на машине разработчика.
4. HANDOFF обновляется в конце каждого рабочего дня: где остановились и что красное.
