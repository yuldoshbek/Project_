# ORBITA — передача работы. Полный контекст для нового агента

Дата: 25.09.2026 · ветка **`block-1-core`** · шаг 0 блока 1 закончен, все проверки
на машине разработки зелёные · удалённый `https://github.com/yuldoshbek/Project_.git`

```bash
git clone https://github.com/yuldoshbek/Project_.git
git checkout block-1-core
```

`block-0-foundation` — законченный блок 0, ждёт приёмки; исправлений конвейера от 25.09
в нём нет.
`block-1-core` — текущая работа: шаг 0 закончен, дальше — экраны блока 1 (раздел 9).

Этот файл самодостаточен: прочитав его целиком, можно продолжить работу, не читая
остальной репозиторий. Всё, что здесь написано, проверено по коду, а не по документам.
Если документ и код расходятся — **прав код**; расхождения и дефекты разобраны в
[аудите 25.09](audit/AUDIT-2026-09-25.md).

> **Начать отсюда:** раздел [8](#8-где-мы-остановились) — точное состояние, и раздел
> [9](#9-что-делать-дальше) — следующий шаг. Остальное — контекст, чтобы шаг был верным.

---

## 1. Что это за продукт

**ORBITA — рабочее пространство на двоих.** Заказчик — помощник заместителя директора
Агентства космических исследований и технологий «Ўзбеккосмос» (Узбекистан, Ташкент).
Пользователей ровно два:

| | Помощник | Руководитель (заместитель директора) |
|---|---|---|
| Что делает | вносит и поддерживает данные, грузит таблицы «Ижро», готовит доклады | смотрит, анализирует, **принимает решения**, меняет сроки |
| Где работает | ноутбук — основное, телефон — захват на ходу | **iPhone — основное**, ноутбук и большой монитор — разбор и совещания |

Ограничений между ними нет: оба видят всё. Роль подписывает действие в журнале, а не
прячет данные. Сотрудники агентства в систему **не входят** — они живут в справочнике как
ответственные.

Система отвечает руководителю на один вопрос: **что требует моего внимания сейчас и какое
решение я принимаю.**

**Два свойства продукта определяют всё остальное:**

1. **Презентационный слой — это и есть система.** Не понял картину за полминуты с
   телефона — задача не выполнена, сколько бы функций ни работало.
2. **Стоимость ввода определяет, выживет ли продукт.** Данные вносит один человек между
   делом. Что можно вычислить — вычисляется, а не вводится.

**Десять разделов**, у каждого один вопрос:

| Раздел | Вопрос | Блок |
|---|---|---|
| Пульт | что требует внимания сейчас по всем разделам | 1 |
| Программы | где мы по многолетним программам и что должно случиться до конца года | 1 |
| Проекты | где мы по каждому проекту и подпроекту | 1 |
| Задачи | что делать, кому и к какому сроку | 1 |
| Ижро | что на контроле, что горит, работает ли ответственный, из-за кого сорвётся | 2 |
| Взаимодействие | кто кого ждёт: организации, письма, соглашения | 2 |
| Доклады и мероприятия | готовы ли мы к дате и кто задерживает | 2 |
| Идеи и карты | что предложено и как это связано | 3 |
| Календарь | что и когда наступит, где тесно | 1 |
| Управление | справочники, загрузка, обход, ссылки доступа | 0 ✅ |

**«Ижро»** (узб. «исполнение») — реестр поручений вышестоящих органов: Администрации
Президента, Кабинета Министров, законодательных актов. Приходит контрольными таблицами
Word на узбекской кириллице. Это самая ценная часть предметной области.

---

## 2. Как устроена работа с заказчиком

- Работаем **крупными блоками**, не тикетами. Один блок — одна ветка — один PR.
- Цикл блока: **экран → API под утверждённый экран → тест визуала → тест кода → отчёт**.
  Порядок «экран первым» введён 20.09 по итогам аудита: прежде писали API вперёд экранов и
  получили 45 эндпоинтов, из которых интерфейс вызывал 5.
- **Объём блока не меняется посреди блока.** Новое требование записывается и обсуждается
  на приёмке.
- Приёмка: заказчик смотрит превью PR, даёт замечания **одним пакетом**, после правок
  нажимает выкладку.
- **Ключи вводит только заказчик.** Разработка их не видит. Не предлагать вводить ключи.
- Заказчик пишет быстро и с опечатками — читать по смыслу, не переспрашивать по мелочам.
- Заказчик просил **не запускать десять агентов и десять окружений**. Работать самому.

### Что заказчик решил (и это не пересматривается без нового ADR)

| Решение | Где записано |
|---|---|
| Облако сейчас (Netlify + Vercel + Neon), переезд на сервер агентства позже | ADR-0028 |
| Вход **по личной ссылке**, без паролей и экранов входа | ADR-0029 |
| Стек интерфейса: React 19, Tailwind 4, shadcn/ui, Motion, Recharts | ADR-0030 |
| Обновление **опросом**, а не постоянным соединением | ADR-0034 |
| Расписание — в GitHub Actions, не на площадке | ADR-0035 |
| Темы: светлая основная + приглушённая сланцево-синяя («не очень тёмная») | ADR-0030 |
| Реальные данные агентства живут в облаке — риск назван, решение его | ADR-0028 |
| Бот SETA работает отдельно; фундамент синхронизации заложить, но не подключать | план, блок 3 |
| Репозиторий **публичный**; ключей в нём нет | инвариант 12 |

### Чего не делать (снято решением заказчика)

Redis, постоянный воркер, ClamAV, Gotenberg, MinIO, вход по паролю, гриф и выдача наружу,
разграничение доступа между пользователями, встречи, Google-календарь, внешние модели ИИ,
собственный Telegram-бот, учёт трудозатрат в часах, ЭЦП и документооборот, публичный
портал, конструктор отчётов.

---

## 3. Карта репозитория

Корень: `D:\Проектная карта` (Windows). Путь содержит пробел и кириллицу — **всегда
брать в кавычки**.

```
CLAUDE.md              ← правила проекта. Агент читает первым
README.md              ← витрина: разделы, документы, запуск
Makefile / make.ps1    ← команды. На Windows — ./make.ps1 <цель>
docker-compose.yml     ← только postgres, порт 55433
.env / .env.example    ← окружение; .env в git не попадает
netlify.toml           ← интерфейс + прокси /api на Vercel

tz/TZ-ORBITA-v2.0.md   ← ПЕРВОИСТОЧНИК ТРЕБОВАНИЙ. Всё, что ему противоречит, — баг
tz/archive/            ← ТЗ v1.0, не действует

docs/
  PLAN.md              ← базовый план: 4 блока и критерии приёмки. Меняется только на приёмке
  CONTEXT.md           ← словарь: термины употребляются строго в его значении
  ARCHITECTURE.md      ← как устроена система и конвейер (местами отстаёт от кода — см. аудит)
  SETUP.md             ← мастер настройки площадок для заказчика (20–30 мин)
  RUNBOOK.md           ← эксплуатация: выкладка, откат, копии, ссылки
  OPEN-QUESTIONS.md    ← открытые вопросы V1–V10 и принятые допущения
  HANDOFF.md           ← этот файл
  adr/                 ← 35 решений. Отменённые помечены, но не удалены
  audit/               ← ГЛАВНОЕ ЧТЕНИЕ ПЕРЕД РАБОТОЙ (см. ниже)
  reports/BLOCK-0.md   ← отчёт блока 0 + 12 снимков экрана в block-0/
  archive/             ← документы до 20.09.2026, НЕ ДЕЙСТВУЮТ
  integration/         ← договор с ботом SETA (67 КБ, читать выборочно)

backend/               ← FastAPI, Python 3.12, uv
frontend/              ← React 19, Vite, TypeScript
scripts/
  doctor.py            ← `make doctor`: проверка машины перед работой
  check_docs.py        ← обязательные документы и целостность ссылок
  neon.sh              ← ветки базы Neon
data/                  ← настоящие таблицы «Ижро». ПУСТА (файлы у заказчика)
design/                ← orbita-design-system.html, 2,7 МБ. НЕ ЧИТАТЬ ЦЕЛИКОМ
.github/               ← 5 workflow, правила ветки main, шаблоны
.claude/               ← команды /block /report /verify и скиллы процесса
```

### Что читать и в каком порядке

1. Этот файл.
2. `docs/audit/AUDIT-2026-09-20.md` — **состояние кода против ТЗ**. Без него легко
   опереться на то, что уже признано долгом.
3. `docs/audit/PLAN-10X.md` — что переписывается, что рефакторится, что не трогать.
4. `tz/TZ-ORBITA-v2.0.md`, раздел 3 (модель) и 4 (сигналы) — под них идёт текущая работа.
5. `CLAUDE.md` — правила и инварианты.
6. `docs/audit/LIMITS-AND-COST.md` — лимиты площадок и расход токенов.

**Не читать целиком:** `design/*.html`, `frontend/package-lock.json`, `backend/uv.lock`,
`docs/integration/*.md`, `docs/archive/**`.

---

## 4. Карта кода

### Backend (`backend/app`) — слоями, зависимости строго сверху вниз

`api → jobs → services → adapters → repos → domain`. Обратные импорты запрещены и
проверяются import-linter (`uv run lint-imports`).

```
main.py            сборка приложения: настройки, логи, база, ошибки, роутеры
settings.py        ORBITA_*; sqlalchemy_url, connect_args (ssl, statement_cache_size)
observability.py   structlog + X-Request-ID сквозь весь запрос
access_cli.py      выпуск первой ссылки: python -m app.access_cli assistant
seed.py            наполнение справочников  ⚠ УСТАРЕЛ, см. раздел 8
jobs/
  registry.py      «работа за период делается один раз» — идемпотентность
  handlers.py      3 задачи: morning-summary, daily-snapshot, deadline-check
  run.py           make job n=<имя>
api/
  router.py        /api/v1, проверка доступа ОДИН раз на весь роутер
  transaction.py   CommitOnSuccess: транзакция закрывается ДО отправки ответа ⭐
  security.py      get_current_user по cookie; require_assistant
  errors.py        RFC 9457 problem+json, один формат на все отказы
  deps.py          сессия и настройки
  routes/
    health.py      /api/health (живость+коммит), /api/ready (база)
    access.py      /api/access/{token}, /api/me, перевыпуск, устройства
    internal.py    /internal/jobs/{name} — вход расписания, секрет в заголовке
    dictionaries.py /api/v1/dictionaries, шаблон вех, организации, пороги
services/
  access.py        выпуск ссылки, открытие сессии, опознание ⭐
  audit.py         журнал пишется САМ, обработчиками сессии ⭐
  codes.py         PRJ-2026-001: номер по числовому хвосту, повтор при гонке
  dictionaries.py  чтение справочников
  metrics.py       СЕРВИС ПОКАЗАТЕЛЕЙ: пороги → снимок → расчёт; ladder и what_if
adapters/          ПУСТО. FileStorage/PushSender — блоки 1–2
repos/
  base.py          Base, UUIDPrimaryKey, Timestamps, Versioned 🆕
  database.py      движок и сессии
  models/          30 таблиц (leader_questions — с 25.09)
  attention.py     снимок для лестницы: read-модель Пульта, SQL только здесь
domain/            правила, без FastAPI и SQLAlchemy
  attention.py     ЛЕСТНИЦА ВНИМАНИЯ: правило, порядок, build_ladder, with_due_changes
  ijro.py          разбор таблиц «Ижро», 551 строка ⭐ ЛУЧШИЙ МОДУЛЬ, НЕ ТРОГАТЬ
  dictionaries.py  статусы, виды, роли, ключи порогов
  projects.py      готовность, отставание, проверки
  tasks.py         переходы статусов, просрочка
  milestones.py    статус вехи, опоздание, сдвиг
  decisions.py     решения руководителя 🆕
  cycles.py        годовые циклы 🆕
  access.py        токены, отпечатки, cookie __Host-
  people.py        роли; clock.py — «сегодня» по Ташкенту
  errors.py        доменные ошибки → коды ответа в api/errors.py
  comments.py      лента «Ижро»; checklists.py — прогресс
  audit.py         что журналируется
```

⭐ — решения, оплаченные измерениями. Не переписывать: обоснование в docstring файла.
🆕 — создано 20.09 в текущей работе (`block-1-core`, коммит `561e1b6`).

### Frontend (`frontend/src`) — 31 файл

```
main.tsx                 QueryClient (опрос 15 с), Motion, тема, роутер
app/
  App.tsx                сессия → оболочка → Outlet
  router.tsx             маршруты кодом из sections.ts
  sections.ts            ЕДИНЫЙ СПИСОК 10 разделов: id, значок, блок, на телефоне ли
  device.ts              phone ≤767 / laptop / monitor ≥1800
  theme.tsx              system | light | dim, атрибут data-theme на <html>
  session.ts             useCurrentUser, useHealth
  NeedsLink.tsx          «откройте по своей ссылке» — не экран входа
  shell/                 AppShell, TopBar, SideRail, BottomBar, ThemeMenu
shared/
  api/client.ts          fetch, credentials same-origin, ApiError.needsLink (401)
  api/orbita.ts          типы РУКАМИ (генерации нет — см. аудит В1)
  i18n/                  ключи; только ru.ts, узбекские — блок 3
  ui/                    Button, Card (с полем «вопрос»), Signal, States
  time.ts, lib/cn.ts
sections/
  SoonSection.tsx        заглушка с вопросом раздела и номером блока
  management/            ЕДИНСТВЕННЫЙ живой раздел: ссылки, справочники, состояние
styles/
  tokens.css             ЕДИНСТВЕННЫЙ источник цвета; 2 темы; 4 сигнала
  app.css                @theme inline поверх токенов
  contrast.test.ts       36 счётных проверок контраста
e2e/                     13 сценариев Playwright + снимки в docs/reports/block-0/
```

---

## 5. Правила, которые нельзя нарушать

### Язык

- Документация, коммиты, комментарии, сообщения пользователю — **по-русски**.
- Идентификаторы, таблицы, поля, пути API — **по-английски**. Смешивать внутри одного
  идентификатора нельзя.
- **Ни одной строки текста в коде интерфейса** — только ключи перевода.
- Содержание поручений «Ижро» хранится как есть, на узбекской кириллице, **не переводится**.
- Имена работ, шагов и входов в GitHub Actions — **латиницей**. Кириллица в имени входа
  `workflow_dispatch` делает файл невалидным целиком (уже обжигались).

### Инварианты (полный список — CLAUDE.md)

1. **«Просрочено» вычисляется, не хранится.** Как и все ступени лестницы внимания.
2. **Одни цифры везде.** Все показатели считает `services/metrics.py`. «Что если» считает
   тем же кодом и **не пишет в базу ничего**.
3. **Аналитика только от вопроса.** У показателя есть вопрос руководителя, ответ числом,
   дата данных и действие. Нет действия — нет показателя.
4. **У поля один хозяин.** Привоз «Ижро» меняет только поля источника, никогда наши.
5. **Каждое изменение — в журнал**, сервисным слоем, в одной транзакции.
6. **Происхождение данных видно на данных.** Сырое написание ФИО хранится всегда.
7. **Свежесть показывается всегда:** «по таблице от 15.09».
8. **Время:** timestamptz, хранение UTC, показ Asia/Tashkent, просрочка по календарным дням.
9. **Схема — только миграцией Alembic.** `create_all` в рантайме запрещён.
10. **Фоновые задачи идемпотентны.**
11. **Реальные данные — только в рабочем контуре.** Превью от ветки `demo`.
12. **Ключей в репозитории нет** — он публичный.
13. **Доступ по личной ссылке**, проверка в одном месте.
14. **Никаких полей «на будущее»** — только под конкретный критерий приёмки.
15. **У редактируемой записи есть версия** (защита от молчаливой перезаписи).

### Стандарты кода (введены 20.09)

- Комментарий объясняет **почему**, а не что. Решение, оплаченное замером или счётом по
  настоящим данным, записывается рядом вместе с этим наблюдением.
- **Причина, которая есть в ADR, заменяется ссылкой на ADR.** Пересказ ADR в коде создаёт
  второй источник правды — именно так `orbita.ts` начал обещать несуществующую команду.
- Комментарий не обещает того, чего нет.
- Цвет — только токеном, проверяется stylelint.

---

## 6. Как запустить

```bash
make doctor        # ПЕРВОЕ: контейнер, порты, .env, секреты, головы миграций, BOM
make up            # postgres в docker, порт 55433
make migrate       # alembic upgrade head
make seed          # справочники
make dev           # backend :8000, frontend :5173
make test          # pytest + vitest
make e2e           # playwright (нужны поднятые backend и frontend)
make check         # ruff + mypy + import-linter + eslint + stylelint + tsc + документы
make job n=deadline-check
```

На Windows — `./make.ps1 <цель>` (тот же список).

### Особенности Windows, на которых уже теряли время

| Грабли | Лечение |
|---|---|
| `npm` разрешается в `npm.ps1`, который ломается на кавычках в аргументах | вызывать `npm.cmd`; в `make.ps1` уже так |
| `make.ps1` без BOM читается PowerShell 5.1 как ANSI и падает | BOM обязателен; `make doctor` проверяет |
| Bash-инструмент **съедает обратные слэши** в heredoc | писать патч-скрипт файлом (Write) и запускать его, либо `chr(92)` |
| На 55432 стоит PostgreSQL **соседнего проекта SETA** | ORBITA на 55433; `make doctor` проверяет, чей это порт |
| PowerShell: `2>&1` на native exe даёт ложный «сбой» | не перенаправлять stderr — он и так захвачен |
| В выводе Bash-инструмента кириллица бывает «кракозябрами» | это артефакт канала, файлы целы; проверять через PowerShell |

### Окружение (`.env`, 18 ключей)

Обязательные: `ORBITA_SESSION_SECRET` (≥32 символов), `ORBITA_JOBS_SECRET` (≥16, **только
латиница** — едет в заголовке HTTP), `ORBITA_DATABASE_URL` либо части `ORBITA_DB_*`,
`ORBITA_BASE_URL` (из него собираются личные ссылки — при неверном значении ссылка ведёт
в никуда).

### Первый вход в систему

```bash
cd backend && uv run python -m app.access_cli assistant
```

Печатает ссылку один раз. Открыть её в браузере — поставится cookie `__Host-orbita` на 30
дней. Дальше перевыпуск — в разделе «Управление».

---

## 7. Git и конвейер

- Ветки: **`block-1-core`** — текущая работа; **`block-0-foundation`** — законченный
  блок 0 под приёмку. Обе отправлены. Основная — `main`.
- Ветка блока 1 заведена отдельно намеренно: незаконченная схема не должна делать
  красной ветку, которую заказчик принимает.
- Коммиты: `B0: краткое описание в повелительном наклонении`. Тело — почему, а не что.
- `main` защищён: слияние только через PR с зелёными проверками. Правила лежат файлом в
  `.github/rulesets/main.json`, **заказчик должен импортировать их вручную** (Settings →
  Rules → Rulesets → Import).
- В репозитории висят **9 открытых PR от Dependabot** — это нормально.

### Workflow

| Файл | Когда | Что |
|---|---|---|
| `ci.yml` | push в main, любой PR | 5 работ: Документы, Backend, Миграции, Frontend, «Сценарии и снимки» |
| `preview.yml` | PR | ветка базы от `demo` → API на Vercel → интерфейс на Netlify → ссылка в PR |
| `deploy.yml` | по кнопке | входы `action` (deploy/rollback) и `migrate`; сверка коммита, автооткат |
| `jobs.yml` | расписание | 08:30 сводка, 23:50 снимок, ежечасно в рабочее время — сроки |
| `backup.yml` | ночью | снимок базы Neon, проверка, уборка старых |

**Ключи площадок не заведены.** Пока их нет, превью честно пишет в сводку прогона, чего
не хватает, и не падает.

---

## 8. Где мы остановились

Состояние на 25.09.2026. Полный разбор — [аудит 25.09](audit/AUDIT-2026-09-25.md).

### Блок 0 — закончен, ждёт приёмки

[Отчёт](reports/BLOCK-0.md). Пять критериев: три закрыты, два закрыты после исправления,
демо-данные перенесены в блок 1. Конвейер при этом **ни разу не работал в облаке**: PR не
было, CI на ветках блоков не запускался, секреты не заведены. Пять ошибок конвейера,
которые всплыли бы на первой настройке, исправлены 25.09 — **на `block-1-core`**, не на
`block-0-foundation` (коммит `21e9ba2`).

### Блок 1, шаг 0 «основание под ТЗ v2.0» — ЗАКОНЧЕН 25.09.2026

| Коммит | Что |
|---|---|
| `08e5b68` | одна базовая миграция под модели, наполнение по ТЗ 3.9 |
| `26b5e0a` | дефекты аудита: 409 на конфликт записи, токен ссылки не попадает в логи, `--force` у задач |
| `983fa49` | тесты под новую схему и справочники |
| `f87b767` | лестница внимания: даты по Ташкенту, порядок «горит», «ждёт решения», «зависит от чужих»; снимок в `repos/attention.py`, расчёт в домене, «что если» без записи в базу |
| `b753945` | frontend: «Управление» под новые справочники, опрос, бюджет по манифесту |
| `21e9ba2` | конвейер: расписание, выкладка, копия, вход в превью; CI на ветках `block-*` |

**Проверено на машине разработки 25.09:** ruff, mypy, import-linter — чисто; pytest — 241;
`alembic upgrade → check → downgrade base → upgrade` — чисто; CHECK-ограничения в базе и в
моделях совпадают (30 = 30); наполнение дважды — второй раз ничего не добавляет; frontend
— tsc, eslint, stylelint, prettier чисто, vitest 55, сборка 145,5 КБ из 250.

**Не проверено:** Playwright (нужны поднятые backend и frontend) и CI на GitHub — ветка
на 25.09 не отправлена. Это первое, что делается дальше.

### Что изменилось в модели за шаг 0

- **`leader_questions`** — вопрос к руководителю: он и только он ставит строку на ступень
  «ждёт решения» (ТЗ 4, допущение V12). Решения руководителя — ответ на вопрос, а не сам
  вопрос: прежде ступень наполнялась его же решениями «вернуть».
- Регионов **14**, а не 15: 12 областей, Каракалпакстан и город Ташкент (ТЗ 3.1).
- Шаблоны вех — минимальные, допущение V11: ТЗ называет типы, но не вехи.
- `citext` снят; у `users.role` уникальность и проверка значений; название организации
  уникально — это ключ наполнения.
- Пороги сигналов в окружении больше не читаются: `ORBITA_WARN_DAYS` и соседи в старом
  `.env` просто игнорируются.

### Блок 1: Пульт — экран утверждён, API написан (25.09.2026)

| Коммит | Что |
|---|---|
| `94df478` | экран Пульта на вымышленных данных: лестница, решение в одно касание, «Отменить», «С прошлого визита», «Кто держит», «Держим ли сроки»; телефон — пять строк без прокрутки (замер на 390×844) |
| `a621174` | API под экран: `GET /pult`, `POST/DELETE /decisions` (только руководитель), `POST/DELETE /questions` (только помощник); визиты; `python -m app.demo` |
| `b47ba7e` | экран переведён на API, вымышленный сервер в браузере удалён; демо-данные в превью; решение по шрифту в журнале плана |
| следующий | вкладка «Отчёт» (неделя и месяц, печать в PDF), `GET /pult/report`; сценарии Playwright Пульта и снимки в `docs/reports/block-1/`; CI передаёт тестам адрес базы |

**Решения заказчика 25.09:** экран утверждён; пять строк на телефоне важнее шрифта 14 px из
ТЗ 9 — подписи остаются 11–12 px (журнал `PLAN.md`).

**Проверено:** backend 270 тестов, frontend 63, Playwright 22 сценария (среди них «пять
строк на телефоне без прокрутки» замером, решение и «Отменить» через API и базу, отчёт
недели в PDF — 3 страницы A4, текст, без служебных элементов); сборка 153,5 КБ из 250.
Снимки: `docs/reports/block-1/` — Пульт и «Управление» на 390, 1440 и 2560 px в обеих
темах, отчёт недели снимком и PDF.

**Отчёт недели и месяца:** календарная неделя с понедельника и месяц, листание назад.
Итоги — за период по журналу изменений; лестница и «кто держит» — на момент
формирования (снимков прошлых состояний нет, отчёт так и пишет). Печать — браузером в
PDF, приглушённая тема на бумаге сама становится светлой.

**Не сделано в Пульте:** предпросмотр утренней сводки; метка изменений вместо опроса
всего раз в 15 с.

**CI на GitHub впервые запустился на ветке блока 25.09.** Backend падал: тесты и миграции
тестовой базы собирают адрес из частей, а CI передавал только строку подключения — так
было и до правки порта, просто CI на ветках не запускался. Исправлено: CI передаёт
`ORBITA_DB_HOST`/`PORT`/`USER`/`PASSWORD`. Открыт PR #11 «Block 1 core» в `main`.

### Блок 1: Проекты — экран утверждён, API написан (25.09.2026)

Экран утверждён заказчиком 25.09.2026 на вымышленных данных; `frontend/src/sections/
projects/demo.ts` удалён, экран работает на API.

- **Ноутбук и монитор:** три вида — доска по статусам (перетаскивание плитки меняет
  статус; пауза и отмена — только с причиной), таблица с сортировкой, таймлайн (полоса от
  начала до срока, вехи ромбами, исходный срок чертой, «сегодня»). На мониторе колонка
  «В работе» двойной ширины.
- **Телефон:** список плиток по статусу; таблиц и таймлайна нет (ТЗ 6).
- **Карточка проекта** листом: ступень и отклонение, вопрос руководителю, сроки /
  готовность / отставание / ответственный, «что мешает» с датой, вехи, «что если», статус
  (только помощник), организации, подпроекты, задачи.
- Проект на паузе остаётся в лестнице внимания — так считает и Пульт: забытая пауза
  всплывает как «молчит». На плитке паузы вместо отставания — причина.

**API** (`backend/app/api/routes/projects.py`, сценарии — `services/projects.py`,
read-модель — `repos/projects.py`):

| Путь | Кто | Что |
|---|---|---|
| `GET /api/v1/projects` | оба | весь раздел: плитки, типы с шаблонами, люди для формы |
| `GET /api/v1/projects/{id}` | оба | карточка: вехи со ступенями, организации, подпроекты, задачи, вопрос, последнее решение |
| `POST /api/v1/projects` | помощник | новый проект: название и тип, вехи из шаблона, срок по последней вехе |
| `PUT /api/v1/projects/{id}/status` | помощник | статус; пауза и отмена — с причиной |
| `PUT /api/v1/projects/{id}/impediment` | помощник | «что мешает»; пустая строка — помеху сняли |
| `POST /api/v1/projects/{id}/what-if` | оба | «что если»: ступень, отставание, вехи, счётчики Пульта — **без записи** |
| `PUT /api/v1/projects/{id}/dates` | помощник | «Применить»: новые сроки проекта и вех |

- Числа — только из `services/metrics.py` (инвариант 2): ступень и отклонение — строки
  той же лестницы, что у Пульта; `metrics.progress` — готовность и отставание (у
  завершённого — 100 % и 0); `metrics.moves_count` — переносы по журналу тем же правилом,
  что «Держим ли мы свои сроки?»; `metrics.impediment_stale` — порог из справочника.
- Каждая правка несёт версию, которую видел человек; устаревшая — `409 stale-data`
  (`domain.errors.check_version`), «Применить» проверяет все версии до первой правки.
- Порядок раздела — порядок лестницы, затем идущее по плану по сроку, в конце закрытое.
- Подпроект входит только в программу, программа — ни во что; закрытая программа новых
  подпроектов не принимает.

**Вымышленные данные** (`app/demo.py`): 21 проект экрана + новый «после визита», 70 вех
(явные и из шаблонов), 62 задачи, роли Центра, чужое головное ведомство, переносы через
журнал, сценарий Пульта сохранён. Пульт на них: 20 строк внимания, «по плану» 83,
переносов 5 на 67 дней.

**Ревью 26.09 (четыре взгляда, найденное проверено и исправлено):**

- срок проекта раньше последней вехи шаблона сжимает шаблон под срок (`domain/projects.
  template_dates`); предпросмотр формы показывает те же даты;
- даты вне 2000–2100 и нулевой символ в тексте — отказ словами (422), а не 500;
- гонка «проверили — сняли снимок» в «что если» — 409 «обновите», а не 500;
- «что мешает» устаревает по календарным дням Ташкента, а не по часам;
- версия для правки берётся в момент начала правки и «Посчитать», а не в момент
  «Сохранить»/«Применить»: опрос раз в 15 с иначе пропускал бы чужую правку;
- отказ записи в карточке проекта показывается словами, а не проглатывается;
- повтор номера при столкновении больше не пишет в журнал вторую запись «создан»
  (`services/audit` собирает записи заново на каждое сохранение).

**Проверено 26.09:** backend 338 тестов (из них 45 — API Проектов, 13 — правила, 10 —
вымышленные данные), frontend 78, Playwright 31 сценарий через настоящий API; ruff, mypy,
import-linter, eslint, stylelint, prettier; сборка 164,7 КБ из 250.

**Не сделано в Проектах — нужен экран, затем API:** на утверждённом экране нет правки
организаций с ролями, направления, региона и описания. Без роли Центра настоящие проекты
не попадут в «что держит Центр», а без головного ведомства — в «зависит от чужих».
Критерий 1 блока («со всеми полями») без этого не закрыт.

### Известные ограничения

- Ижро в лестнице нет — блок 2, вместе с признаком жизни поручения.
- «Что если» меняет только сроки: зависимостей между работами в ТЗ нет.
- Опрос раз в 15 с оставлен для `/api/me`; «дешёвый запрос с меткой изменений» (ADR-0034) —
  блок 1, вместе с Пультом.
- **Базовая миграция переписана под тем же номером `0001_foundation`.** У кого база уже
  накатывалась прежним вариантом, Alembic её не пересоберёт: нужен `DROP SCHEMA orbita
  CASCADE` в `orbita` и `orbita_test` (или `make reset`). Рабочей базы ещё нет, поэтому
  это касается только машин разработки.

---

## 9. Что делать дальше

Строго по порядку.

1. **SETUP заказчиком** — [SETUP](SETUP.md): два новых секрета (`PREVIEW_ACCESS_TOKEN`,
   `PRODUCTION_FIRST_ACCESS_TOKEN`) и три обязательные настройки Vercel и Netlify. После
   этого превью PR открывается на iPhone, и руководитель смотрит Пульт сам.
2. **Пульт — довести:** предпросмотр утренней сводки; метка изменений вместо опроса.
3. **Проекты — довести карточку:** правка организаций с ролями, направления, региона и
   описания (экран → утверждение → API); без неё не закрыт критерий 1 и срез «что держит
   Центр» на настоящих данных.
4. **Дальше по блоку 1 тем же порядком «экран → API»:** Задачи (создание строкой),
   Программы, Календарь, Захват. Как должен выглядеть экран —
   `docs/audit/PLAN-10X.md`, раздел «Визуал: что значит 10x на экране».

---

## 10. Главные находки аудита (чтобы не наступить снова)

Полностью — `docs/audit/AUDIT-2026-09-20.md`. Коротко:

- **Диагноз:** ORBITA v3 была фундаментом под ТЗ v1.0, переименованным в фундамент под
  ТЗ v2.0. 45 эндпоинтов при 5 используемых, 26 таблиц при 8 ненужных, 362 теста при ~208
  по снятому объёму. Именно это сейчас и исправляется.
- **Документы обещали шесть вещей, которых в коде нет:** сервис показателей, единица
  работы с исходящей очередью, генерация клиента из OpenAPI, поле версии, порт
  `FileStorage`, shadcn/ui. Три из шести закрываются текущей работой.
- **Опрос раз в 15 секунд задан по умолчанию для всех запросов** (`main.tsx:28`) — это
  ≈276 тыс. вызовов функции в месяц для двух человек и база, которая не засыпает весь
  рабочий день. ADR-0034 предписывал «дешёвый запрос с меткой изменений» — не сделано.
  Исправление даёт 276 тыс. → 65 тыс. без разницы для пользователя.
- **Холодный старт ни разу не измерен** при нормативе ТЗ «первый экран меньше 2,5 с».
  Замерить до первой выкладки.

---

## 11. Открытые вопросы и что нужно от заказчика

Живой список — `docs/OPEN-QUESTIONS.md` (V1–V10). Правило: упёрлись в неотвеченный вопрос
— **не угадывать**: дописать туда и работать по зафиксированному допущению.

Ждёт заказчика прямо сейчас:

1. **Настоящие таблицы «Ижро»** — `data/` пуста. Без них блок 2 можно разрабатывать по
   образцам, но **принимать нельзя**.
2. **Тариф Vercel** (V10): условия бесплатного тарифа описывают личные некоммерческие
   проекты, а ORBITA — система государственного органа.
3. **Тариф базы**: развилка «платим за то, что база не спит» против «принимаем задержку
   первого входа». Решается замером, а не спором.
4. **Импорт правил ветки `main`** и прохождение `docs/SETUP.md`.

---

## 12. Короткий чек-лист для первого часа

```
1. git fetch && git checkout block-1-core   → текущая работа здесь
2. make doctor                              → всё зелёное?
3. прочитать docs/audit/AUDIT-2026-09-25.md → итог и план ускорения
4. git log --oneline -8                     → сверить с разделом 8 этого файла
5. make check && make test                  → зелёное до первой правки
6. раздел 9 этого файла                     → следующий шаг
```

**Три вещи, которые чаще всего делают неправильно:**

1. Пишут API раньше экрана. Порядок обратный: экран → заказчик смотрит → API.
2. Заводят поле «пригодится позже». Инвариант 14 — только под конкретный критерий приёмки.
3. Считают показатель на месте, а не в `services/metrics.py`. Инвариант 2 существует не
   для сегодняшнего дня.
